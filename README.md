# Vinum collection frontend

# Running tasks

Run project for dev:

```shell
pnpm run dev
```

Lint code project:

```shell
pnpm run lint
```

Format code project:

```shell
pnpm run format
```

Build project:

```shell
pnpm run build
```
